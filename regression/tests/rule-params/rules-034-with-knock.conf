#
# Single rule parameter configuration
#

. ${REGRESSDIR}include/regression.conf

#INTERFACE
interface eth0 myeth0
if ipv6_capable; then
	server tftp accept with knock admin
	ipv6 server ssh accept with knock admin
fi
	ipv4 server ssh accept with knock admin
#END INTERFACE
