#
# This tests the behaviour of the "mark" to ensure it creates correct
# IPv4/IPv6 output despite use of a '.' in the chain name.
#
version 5

if [ ! "$IP6TABLES_CMD" ]
then
ipv4() {
	"$@"
}
ipv6() {
	"$@"
}
fi

mark 3 OUTPUT mark 1 uid 9998

interface eth0 test
	policy deny
