version 5

if [ ! "$IP6TABLES_CMD" ]
then
ipv4() {
	"$@"
}
ipv6() {
	"$@"
}
fi

interface eth0 myeth0
	policy deny
	ipv6 client tftp accept src not "any.old.rubbish1"
	ipv6 client ssh accept src not "any.old.rubbish1"
