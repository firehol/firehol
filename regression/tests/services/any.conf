#
# Single service configuration
#
# This test was manually created because the syntax differs from the
# common syntax
#
version 5

. ${REGRESSDIR}include/regression.conf

interface eth0 myeth0
	server any aname1 accept

interface eth1 myeth1
	client any aname2 accept

router e2toe3 inface eth2 outface eth3
	server any aname3 accept

router e4toe5 inface eth4 outface eth5
	client any aname4 accept
